diff --git a/Sources/AetherEngine/AetherEngine+IndependentAudioSource.swift b/Sources/AetherEngine/AetherEngine+IndependentAudioSource.swift
new file mode 100644
index 0000000..152aed1
--- /dev/null
+++ b/Sources/AetherEngine/AetherEngine+IndependentAudioSource.swift
@@ -0,0 +1,147 @@
+import Foundation
+
+/// Stable, non-AVFoundation description of the audible option selected by a
+/// probe-free native HLS item.
+public struct AetherRemoteHLSAudioSelection: Sendable, Equatable {
+    public let displayName: String?
+    public let language: String?
+    public let optionOrdinal: Int?
+
+    public init(
+        displayName: String?,
+        language: String?,
+        optionOrdinal: Int?
+    ) {
+        self.displayName = displayName
+        self.language = language
+        self.optionOrdinal = optionOrdinal
+    }
+}
+
+/// Everything middleware needs to prepare an independent finite HLS VOD
+/// cursor. The request contains no AVPlayer or AVMediaSelectionOption objects.
+public struct AetherRemoteHLSAudioRequest: Sendable, Equatable {
+    public let url: URL
+    public let httpHeaders: [String: String]
+    public let selection: AetherRemoteHLSAudioSelection
+
+    public init(
+        url: URL,
+        httpHeaders: [String: String],
+        selection: AetherRemoteHLSAudioSelection
+    ) {
+        self.url = url
+        self.httpHeaders = httpHeaders
+        self.selection = selection
+    }
+}
+
+public enum AetherIndependentAudioSelection: Sendable, Equatable {
+    case ffmpeg(TrackInfo)
+    case remoteHLS(AetherRemoteHLSAudioSelection)
+}
+
+/// Stable source facts captured from one loaded AetherEngine session.
+///
+/// The value can create a fresh demuxer, but never exposes or reuses the
+/// playback cursor. Hybrid owns decoding, resampling, back-pressure, and
+/// cancellation after it receives this source.
+public struct AetherIndependentAudioSource: @unchecked Sendable {
+    public enum Kind: Sendable, Equatable {
+        case url
+        case custom
+        case remoteHLS
+    }
+
+    public let kind: Kind
+    public let durationSeconds: Double
+    public let isSeekable: Bool
+    public let selection: AetherIndependentAudioSelection
+
+    public var selectedAudioTrack: TrackInfo? {
+        guard case .ffmpeg(let track) = selection else {
+            return nil
+        }
+        return track
+    }
+
+    public var remoteHLSRequest: AetherRemoteHLSAudioRequest? {
+        guard case .remoteHLS(let request) = backing else {
+            return nil
+        }
+        return request
+    }
+
+    fileprivate enum Backing {
+        case url(URL, headers: [String: String])
+        case custom(IOReader, formatHint: String?)
+        case remoteHLS(AetherRemoteHLSAudioRequest)
+    }
+
+    private let backing: Backing
+
+    fileprivate init(
+        backing: Backing,
+        durationSeconds: Double,
+        isSeekable: Bool,
+        selection: AetherIndependentAudioSelection
+    ) {
+        self.backing = backing
+        self.kind = {
+            switch backing {
+            case .url: .url
+            case .custom: .custom
+            case .remoteHLS: .remoteHLS
+            }
+        }()
+        self.durationSeconds = durationSeconds
+        self.isSeekable = isSeekable
+        self.selection = selection
+    }
+
+    /// Opens a caller-owned demuxer. Supplying the instance first lets the
+    /// caller make cancellation visible before a network open begins.
+    public func openDemuxer(_ demuxer: Demuxer) throws {
+        guard isSeekable else {
+            throw AetherIndependentAudioSourceError.sourceNotSeekable
+        }
+
+        do {
+            switch backing {
+            case .url(let url, let headers):
+                try demuxer.openIndependent(url: url, extraHeaders: headers)
+            case .custom(let reader, let formatHint):
+                guard let independent = reader.makeIndependentReader() else {
+                    throw AetherIndependentAudioSourceError.independentReaderUnavailable
+                }
+                try demuxer.openIndependent(reader: independent, formatHint: formatHint)
+            case .remoteHLS:
+                throw AetherIndependentAudioSourceError.remoteHLSPreparationRequired
+            }
+        } catch {
+            demuxer.close()
+            throw error
+        }
+        guard demuxer.isSourceSeekable else {
+            demuxer.close()
+            throw AetherIndependentAudioSourceError.sourceNotSeekable
+        }
+    }
+
+    /// Convenience for clients that do not need to register cancellation
+    /// before open.
+    public func makeDemuxer() throws -> Demuxer {
+        let demuxer = Demuxer()
+        try openDemuxer(demuxer)
+        return demuxer
+    }
+}
+
+public enum AetherIndependentAudioSourceError: Error, Sendable, Equatable {
+    case noActiveSession
+    case liveOrDVRUnsupported
+    case sourceNotSeekable
+    case selectedAudioTrackUnavailable
+    case independentReaderUnavailable
+    case remoteHLSPreparationRequired
+}
diff --git a/Sources/AetherEngine/Demuxer/Demuxer.swift b/Sources/AetherEngine/Demuxer/Demuxer.swift
index 1ae003c..80c1582 100644
--- a/Sources/AetherEngine/Demuxer/Demuxer.swift
+++ b/Sources/AetherEngine/Demuxer/Demuxer.swift
@@ -163,7 +163,9 @@ public final class Demuxer: @unchecked Sendable {
     var avioBytesFetched: Int64 { avioProvider?.cumulativeBytesFetched ?? 0 }
 
     // Forward-only custom sources report false.
-    var isSourceSeekable: Bool { avioProvider?.isSeekable ?? true }
+    public var isSourceSeekable: Bool { avioProvider?.isSeekable ?? true }
+
+    public init() {}
 
     /// Timestamp of last unplanned reconnect (drop/stall, not a seek).
     /// Live producer correlates with backward source-PTS reset to detect
@@ -307,6 +309,12 @@ public final class Demuxer: @unchecked Sendable {
         }
     }
 
+    /// Open a fresh seekable-VOD cursor without exposing AetherEngine's
+    /// playback-oriented demux tuning to middleware clients.
+    public func openIndependent(url: URL, extraHeaders: [String: String] = [:]) throws {
+        try open(url: url, extraHeaders: extraHeaders, profile: .playback, isLive: false)
+    }
+
     // MARK: - Open Strategies
 
     private func openLocal(url: URL) throws {
@@ -347,6 +355,11 @@ public final class Demuxer: @unchecked Sendable {
         try openWithProvider(bridge, inputFormat: inputFormat, isLive: isLive)
     }
 
+    /// Open a fresh cursor over a caller-owned independent IOReader.
+    public func openIndependent(reader: IOReader, formatHint: String? = nil) throws {
+        try open(reader: reader, formatHint: formatHint, profile: .playback, isLive: false)
+    }
+
     /// A remote disc image (ISO 9660 / UDF / BDMV) by URL extension. Gates the HTTP disc-adapter
     /// path so a normal media URL keeps the optimized streaming AVIOReader open with no probe cost.
     static func isDiscImageURL(_ url: URL) -> Bool {
@@ -555,6 +568,15 @@ public final class Demuxer: @unchecked Sendable {
         return ctx.pointee.start_time
     }
 
+    /// Zero-based source-timeline origin for independent readers.
+    public var formatStartTimeSeconds: Double {
+        let value = formatStartTime
+        guard value != Int64.min else {
+            return 0
+        }
+        return Double(value) / Double(AV_TIME_BASE)
+    }
+
     /// Index of the best video stream, or -1.
     /// Clamped: av_find_best_stream returns AVERROR_STREAM_NOT_FOUND (-1381258232)
     /// on failure, not -1; normalize to -1 to avoid garbage in logs.
@@ -595,7 +617,7 @@ public final class Demuxer: @unchecked Sendable {
         return -1
     }
 
-    func audioTrackInfos() -> [TrackInfo] {
+    public func audioTrackInfos() -> [TrackInfo] {
         guard let ctx = formatContext else { return [] }
         var tracks: [TrackInfo] = []
         for i in 0..<Int(ctx.pointee.nb_streams) {
@@ -840,7 +862,7 @@ public final class Demuxer: @unchecked Sendable {
         return 0
     }
 
-    func stream(at index: Int32) -> UnsafeMutablePointer<AVStream>? {
+    public func stream(at index: Int32) -> UnsafeMutablePointer<AVStream>? {
         guard let ctx = formatContext, index >= 0, index < ctx.pointee.nb_streams else {
             return nil
         }
@@ -854,7 +876,7 @@ public final class Demuxer: @unchecked Sendable {
     /// `pacing`, when >= 0 and not already in `keep`, is delivered at `AVDISCARD_NONKEY` instead of
     /// being dropped: keyframes only, enough to give a side reader a read-position control point
     /// between sparse target packets (#230). -1 keeps the original all-or-nothing behavior.
-    func discardAllStreamsExcept(_ keep: Set<Int32>, pacing: Int32 = -1) {
+    public func discardAllStreamsExcept(_ keep: Set<Int32>, pacing: Int32 = -1) {
         accessLock.lock()
         defer { accessLock.unlock() }
         guard let ctx = formatContext else { return }
@@ -903,7 +925,7 @@ public final class Demuxer: @unchecked Sendable {
         return result
     }
 
-    func readPacket() throws -> UnsafeMutablePointer<AVPacket>? {
+    public func readPacket() throws -> UnsafeMutablePointer<AVPacket>? {
         accessLock.lock()
         defer { accessLock.unlock() }
         guard let ctx = formatContext else { return nil }
@@ -981,10 +1003,11 @@ public final class Demuxer: @unchecked Sendable {
 
     /// Seek via avformat_seek_file (not av_seek_frame: assertion failures
     /// in matroskadec.c with nested elements).
-    func seek(to seconds: Double) {
+    @discardableResult
+    public func seek(to seconds: Double) -> Bool {
         accessLock.lock()
         defer { accessLock.unlock() }
-        guard let ctx = formatContext else { return }
+        guard let ctx = formatContext else { return false }
         let timestamp = Int64(seconds * Double(AV_TIME_BASE))
         let ret = avformat_seek_file(ctx, -1, Int64.min, timestamp, Int64.max, 0)
         if ret < 0 {
@@ -994,6 +1017,7 @@ public final class Demuxer: @unchecked Sendable {
         }
         avformat_flush(ctx)  // prevents assertion failures in matroskadec.c
         lastReadClipIdx = -1  // AE#105: post-seek reads may land mid-clip; require a fresh clean crossing
+        return ret >= 0
     }
 
     /// Seek on one stream's native timestamp axis, never before `timestamp`.
@@ -1301,11 +1325,11 @@ public final class Demuxer: @unchecked Sendable {
 
     /// Fast lock-free unblock: AVIO read callback returns -1, av_read_frame returns
     /// at once. No resource freeing. Call before close() when cancelling a pump.
-    func markClosed() {
+    public func markClosed() {
         avioProvider?.markClosed()
     }
 
-    func close() {
+    public func close() {
         avioProvider?.markClosed()  // unblocks av_read_frame (tvOS suspends threads in background)
         accessLock.lock()
         if formatContext != nil {
